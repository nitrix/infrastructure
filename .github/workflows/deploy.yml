name: deploy
on:
    push:
        branches:
            - master
jobs:
    setup:
        name: Deploying the infrastructure
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        
        - name: Resolving secrets into files
          env:
            NITRIX_ME_CRT: ${{ secrets.NITRIX_ME_CRT }}
            NITRIX_ME_PEM: ${{ secrets.NITRIX_ME_PEM }}
          shell: bash
          run: |
            echo $NITRIX_ME_CRT > conf/ssl/nitrix.me.crt
            echo $NITRIX_ME_PEM > conf/ssl/nitrix.me.pem

        - name: Updating stack
          shell: bash
          env:
            SWARM_SSH_IP: ${{ secrets.SWARM_SSH_IP }}
            SWARM_SSH_USER: ${{ secrets.SWARM_SSH_USER }}
            SWARM_SSH_KEY: ${{ secrets.SWARM_SSH_KEY }}
          run: |
            eval `ssh-agent -s`
            echo "$SWARM_SSH_KEY" | ssh-add -
            mkdir -p ~/.ssh
            ssh-keyscan -t rsa $SWARM_SSH_IP >> ~/.ssh/known_hosts
            rsync -r . $SWARM_SSH_USER@$SWARM_SSH_IP:/tmp/infrastructure --delete
            ssh $SWARM_SSH_USER@$SWARM_SSH_IP docker stack deploy -c /tmp/infrastructure/docker-compose.yml prod